https://digvps.com/depoly/monitoring

```
ssh -L 3001:localhost:3000 \
    -i ~/.ssh/my-server \
    ubuntu@103.122.116.119 \
    -N -f

ssh -L 9001:localhost:9090 \
    -i ~/.ssh/my-server \
    ubuntu@103.122.116.119 \
    -N -f

ssh -L 9093:localhost:9093 \
    -i ~/.ssh/my-server \
    ubuntu@103.122.116.119 \
    -N -f

```

```
pkill -f "ssh -L 9001:localhost:9090"
pkill -f "ssh -L 9093:localhost:9093"
pkill -f "ssh -L 3001:localhost:3000"
```

```
sudo vim /etc/netplan/50-cloud-init.yaml
```

```
sudo netplan apply
```

刪掉沒在用的 docker images / containers / networks / build cache

```
docker system prune -af
```

```
go build -o my-exporter main.go
./my-exporter
```

```bsah
~/my-exporter/alertmanager-0.27.0.linux-amd64
python3 webhook.py
```

```
source venv/bin/activate
```

Tempo

```
{ resource.service.name = "unknown_service" }
```
